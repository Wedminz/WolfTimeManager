<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="inbox.wolf.alex.wolftimemanager">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.activity.ProjectList">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.activity.CreateProject"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.CreateTimer"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.AppSettings"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.ResetPassword"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.EditProject"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.EditTimer"
            android:theme="@style/AppDialogTheme"/>
        <activity android:name=".main.activity.SingIn"/>
        <activity android:name=".main.activity.SingUp"/>
    </application>

</manifest>